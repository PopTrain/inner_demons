// Intentionally empty for now - no privileged main-process APIs are exposed
// to the renderer yet. Add contextBridge.exposeInMainWorld(...) here once
// the game needs native functionality (native save file I/O, window controls).
