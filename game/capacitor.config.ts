import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.magickahat.innerdemons",
  appName: "Inner Demons: Fairbound Crypt",
  webDir: "dist",
  backgroundColor: "#000000",
};

export default config;
